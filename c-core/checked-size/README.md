# checked_size

Decide whether n * size fits in a size_t and write the product only when it does

```c
int checked_size(size_t n, size_t size, size_t *out)
```

**Difficulty:** 3/5
**Allowed functions:** none

Solved in practice.
Validated 2 times. Earlier versions are in this file's git history.

[Read the full exercise on Grademe](https://grademe.io/app/exercise/checked-size)
